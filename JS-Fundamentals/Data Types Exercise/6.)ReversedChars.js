//Task: Write a program that takes 3 parameters (characters) and prints them in reversed order with a space between them.

//Solution:

function reversedChars(char1, char2, char3) {
  let print = char3 + " " + char2 + " " + char1;
  console.log(print);
}
reversedChars(
  "A",

  "B",

  "C"
);

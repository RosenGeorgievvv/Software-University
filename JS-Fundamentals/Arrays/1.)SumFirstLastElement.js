//Task: Write a function that receives an array of numbers and prints the sum of the first and last element in that array.

//Solution:

function sumElements(input) {
  console.log(input[0] + input[input.length - 1]);
}
sumElements([20, 30, 40]);
